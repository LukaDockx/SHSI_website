{
  "name": "shsi-attendance-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Static browser app for generating SHSI attendance check sheets from local CSV exports.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "4.9.5"
  }
}
